<?php
/**
 * Created by PhpStorm.
 * User: rbt
 * Date: 19-5-10
 * Time: 下午5:37
 */

namespace PhpDemo\Advance\Algorithm;

/**
 * 二叉树
 */
class BinaryTree
{
    /**
     * 前序遍历
     * @param BinaryTree $binaryTree
     * @return array
     */
    public static function preorderTraversal(BinaryTree $binaryTree): array
    {
        $arr = [];

        return $arr;
    }

    /**
     * 中序遍历
     * @param BinaryTree $binaryTree
     * @return array
     */
    public static function inorderTraversal(BinaryTree $binaryTree): array
    {
        $arr = [];

        return $arr;
    }

    /**
     * 后序遍历
     * @param BinaryTree $binaryTree
     * @return array
     */
    public static function postorderTraversal(BinaryTree $binaryTree): array
    {
        $arr = [];

        return $arr;
    }

    /**
     * 层次遍历
     * @param BinaryTree $binaryTree
     * @return array
     */
    public static function levelTraversal(BinaryTree $binaryTree): array
    {
        $arr = [];

        return $arr;
    }

    /**
     * 深度优先遍历：事实上就是上面的前序、中序和后序。
     * @param BinaryTree $binaryTree
     * @return array
     */
    public static function depthFirstTraverse(BinaryTree $binaryTree): array
    {
        $arr = [];

        return $arr;
    }

    /**
     * 广度优先遍历：事实上就是上面的层次遍历。
     * @param BinaryTree $binaryTree
     * @return array
     */
    public static function breadthFirstTraversal(BinaryTree $binaryTree): array
    {
        $arr = [];

        return $arr;
    }
}
